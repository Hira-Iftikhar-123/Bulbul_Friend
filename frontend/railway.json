{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci --include=dev && chmod +x ./node_modules/.bin/webpack && npm run build"
  },
  "deploy": {
    "startCommand": "npm run serve",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}